const DB_USERNAME = process.env.DB_USERNAME || '';
const DB_PASS = process.env.DB_PASS || '';
const DB_HOST = 'db';
const DB_PORT = 3306;
const DB_NAME = 'fincloud';

export default { DB_USERNAME, DB_PASS, DB_HOST, DB_NAME, DB_PORT };
